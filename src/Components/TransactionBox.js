import React from 'react';
import { View, Text} from 'react-native';

const TransactionBox = () => {
    return(
        <View style={{marginTop: '10%', height: '53%', marginHorizontal: '5%', borderRadius: 10, width: '90%', backgroundColor: 'skyblue'}}>

        </View>
    );
}

export default TransactionBox;